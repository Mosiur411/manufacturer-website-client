import React from 'react';
import ReactHelmet from '../../hook/ReactHelmet';

const Blog = () => {
    return (
        <div>
            <ReactHelmet>Blog</ReactHelmet>
            This is blog
        </div>
    );
};

export default Blog;